# Archived agents

These packs are retired from the active marketplace. They were earlier
scaffolding or benchmark cases that are not part of the curated, validated set.
Only the agents at the repository root are current and supported:

- `earthscope-gnss-region` — EarthScope GNSS region case (case02).
- `wildfire-smoke-impact-review` — NDP wildfire downwind smoke-impact case.

Packs here are kept for reference/history; revive one by moving it back to the
root and bringing it to the current Agent Blueprint contract.
